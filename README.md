# Generic SMTP Node App

A small Node.js app that sends email through any SMTP provider using Nodemailer.

## Setup

```sh
npm install
cp .env.example .env
```

Edit `.env` with your SMTP credentials.

## Run as an HTTP API

```sh
npm start
```

`npm start` runs:
